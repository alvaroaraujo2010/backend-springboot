spring:
  datasource:
    url: jdbc:mysql://localhost:3306/proyecto_db?useSSL=false&serverTimezone=UTC
    username: root
    password: "ingAlv4r0"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080
